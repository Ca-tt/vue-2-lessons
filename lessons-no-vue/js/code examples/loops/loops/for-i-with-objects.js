/* FOR(I) С ОБЪЕКТАМИ */

let family = {
  mother: 'Olia',
  granny: 'Svetlana',
  father: 'Timur'
}

// Не сработает, но и ошибки не будет. А жаль
for (let index = 0; index < family.length; index++) {
  console.log('- family[i] -', family[i]);
}

/* ============= */